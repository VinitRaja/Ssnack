# Ssnack Spring Boot Project

This is use for selling and borrowing items from your partner 

## Build

To Build the project, run:

  mvn clean install

 ## Run
To run the project, use:

  mvn spring-boot:run
